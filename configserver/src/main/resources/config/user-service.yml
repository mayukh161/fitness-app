spring:
  application:
    name: user-service
  cloud:
    inetutils:
      preferred-networks:
        - 127.0.0
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/fitness_user
    username: root
    password: Sunny@2004
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    hostname: 127.0.0.1
  

